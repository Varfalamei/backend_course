=======================
FastAPI base project
=======================

Installation
------------

Install project (you need conda) by running:

    bash ./workspace.sh
    poetry run uvicorn app.main:app --reload